"""Base chain view widgets.

Only the six ``BT*`` overview widgets survive here; the screen imports them
from :mod:`maxpane_dashboard.widgets.base.overview` directly, so this package
re-exports nothing of its own.
"""
